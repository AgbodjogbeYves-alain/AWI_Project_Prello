{
  "name": "server",
  "private": true,
  "scripts": {
    "start": "meteor run",
    "test": "meteor test --once --driver-package meteortesting:mocha"
  },
  "dependencies": {
    "@babel/runtime": "^7.1.5",
    "asteroid": "^2.0.3",
    "bcrypt": "^3.0.2",
    "meteor-node-stubs": "^0.4.1",
    "rus-diff": "^1.1.0",
    "simpl-schema": "^1.5.3"
  },
  "meteor": {
    "mainModule": {
      "server": "./main.js"
    }
  }
}
