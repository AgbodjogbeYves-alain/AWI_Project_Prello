{
  "name": "server",
  "private": true,
  "scripts": {
    "start": "meteor run",
    "test": "meteor test --once --driver-package meteortesting:mocha"
  },
  "dependencies": {
    "@babel/runtime": "^7.1.2",
    "asteroid": "^2.0.3",
    "bcrypt": "^3.0.2",
    "meteor-node-stubs": "^0.4.1",
    "simpl-schema": "^1.5.3"
  },
  "meteor": {
    "mainModule": {
      "server": "./main.js"
    }
  }
}
